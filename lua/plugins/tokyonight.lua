return {
  {
    "folke/tokyonight.nvim",
    opts = {
      transparent = false,
      -- Without these, sidebars and floats keep an opaque background and the
      -- transparency looks broken rather than intentional.
      -- styles = {
      --   sidebars = "transparent",
      --   floats = "transparent",
      -- },
    },
  },
}
